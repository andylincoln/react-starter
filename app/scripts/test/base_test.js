import chai, {expect} from 'chai';

// Base to establish that the test framework is configured correctly
describe('Test Check' , () => {
  it('renders something', () => {
    expect(true).to.equal.true;
  });
});
